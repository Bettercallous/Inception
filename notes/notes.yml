
wordpress:
    - FastCGI
    - PHP-FPM
    - uri = Uniform Resource Identifier
    - url = Uniform Resource Locator

docker:
    - containerization
    - container vs vm
    - PID 1
    - daemon
    - RUN vs CMD
    - CMD VS ENTRYPOINT

    - docker networks
    - docker volumes
    - docker cache

other:
    - sed


PID 1 and the best practices for writing Dockerfiles.

mariadb and sql

nginx and its config

RUN vs CMD vs ENTRYPOINT

COPY vs ADD

docker caching

volumes and networks

TLS


Check if redis is working:
    - docker exec -it redis redis-cli && type ping, you should get pong
    - docker exec -it redis redis-cli monitor (will output OK)